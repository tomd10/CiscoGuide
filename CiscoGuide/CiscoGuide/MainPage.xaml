<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CiscoGuide"
             x:Class="CiscoGuide.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="{Binding Napoveda}" 
           HorizontalOptions="Center"/>
        <Button Text="Nápověda" Command="{Binding NapovedaCommand}"></Button>
        <Button Text="Nastavení interface" Command="{Binding InterfaceCommand}"></Button>
        <Button Text="Nastavení DHCP" Command="{Binding DHCPCommand}"></Button>
        <Button Text="Nastavení Telnet" Command="{Binding TelnetCommand}"></Button>
    </StackLayout>

</ContentPage>
